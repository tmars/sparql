PREFIX foaf:  <http://xmlns.com/foaf/0.1/>
SELECT *
WHERE {
    ?person foaf:name ?name .
    ?person foaf:position ?position .
    OPTIONAL { ?person foaf:mbox ?email } .
    FILTER REGEX(?name, "^Dean") 
}
ORDER BY ?name






